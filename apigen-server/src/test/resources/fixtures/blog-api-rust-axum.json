{
  "project": {
    "name": "My Rust Axum API",
    "groupId": "my-rust-api",
    "artifactId": "my-rust-api",
    "basePackage": "my_rust_api",
    "version": "0.1.0",
    "modules": {
      "core": true,
      "security": false
    },
    "features": {
      "hateoas": false,
      "swagger": true,
      "softDelete": true,
      "auditing": true,
      "caching": false,
      "docker": true,
      "socialLogin": false,
      "passwordReset": false,
      "mailService": false,
      "fileUpload": false,
      "jteTemplates": false
    },
    "database": {
      "type": "postgresql",
      "name": "appdb",
      "username": "appuser",
      "password": "changeme",
      "port": 5432
    },
    "options": {
      "preset": "cloud",
      "useJwt": true,
      "useRedis": false,
      "useDocker": true
    }
  },
  "target": {
    "language": "rust",
    "framework": "axum"
  },
  "sql": "CREATE TABLE products (\n    id BIGINT PRIMARY KEY,\n    name VARCHAR(255) NOT NULL,\n    description TEXT,\n    price DECIMAL(10,2) NOT NULL,\n    stock INTEGER NOT NULL DEFAULT 0,\n    active BOOLEAN NOT NULL DEFAULT TRUE,\n    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    deleted_at TIMESTAMP\n);\n\nCREATE TABLE categories (\n    id BIGINT PRIMARY KEY,\n    name VARCHAR(100) NOT NULL UNIQUE,\n    slug VARCHAR(100) NOT NULL UNIQUE,\n    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    deleted_at TIMESTAMP\n);"
}
